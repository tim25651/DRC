[project]
name = "drc"
version = "0.0.1"
dependencies = [
    "matplotlib",
    "numpy",
    "pandas",
    "scipy"
]

[tool.setuptools.packages.find]
include= ["drc*"]

[project.scripts]
drc = "drc.cli:main"
drc-test = "drc.test:main"

[tool.isort]
profile = "black"